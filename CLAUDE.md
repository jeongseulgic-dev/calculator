# 계산기 사이트

금융/건강/수학/생활 계산기를 모아놓은 정적 사이트(순수 HTML/CSS/JS, 빌드 도구 없음).
calculator.net을 UX(기능 구성)만 참고하고 UI는 독자적으로 만들었으며, 한국 사용자 기준
계산 로직(2026년 4대보험 요율, 만 나이 통일법, WHO 아시아-태평양 BMI 기준 등)을 직접
구현한다.

- 배포: GitHub(`jeongseulgic-dev/calculator`) → Cloudflare Pages 자동 배포 (`main` push 시)
- 목표: 커스텀 도메인 연결 후 Google AdSense로 운영
- 성능 목표: LCP 2.5초 · INP 200ms · CLS 0.1 이내 (PageSpeed 기준). 외부 스크립트를
  추가하기 전에 이 목표를 해치지 않는지 먼저 확인한다.

## 디자인 원칙

- 계산기 자체(입력 패널 + 계산 결과 영역)는 레트로 카시오 계산기 + 영수증 모티프를
  유지한다 (`.calc-panel`, `.receipt-strip`, `.receipt`). 이건 의도된 정체성이다.
- 반면 설명/정보성 콘텐츠(계산기 설명 섹션, 소개·문의·정책 페이지 등)는 영수증 톤을
  쓰지 않는다. 흰 배경 + 얇은 테두리 + 은은한 그림자 카드(`.info-block`, `.cat-card`류)에
  핑크 포인트 컬러로 강조하는 톤을 쓴다.
- 새 UI 요소(버튼, select 등)에 폰트를 임의로 bold 처리하지 않는다 — 기본 굵기(400) 유지.

## 콘텐츠 원칙

- 모든 문장은 새로 쓴다. calculator.net을 비롯한 외부 사이트를 번역·복붙하지 않는다.
- 계산기 페이지의 공식·기준값·색상은 반드시 해당 `js/*-calc.js`의 실제 로직을 먼저
  확인하고 그대로 반영한다. 지어내지 않는다.
- 담백한 설명체를 쓴다. 광고성 문구·과장된 표현 금지.
- 페이지가 특정 공식 요율·기준치·학술 공식(4대보험 요율, WHO 기준, 논문 공식 등)을
  명시할 땐 안내 섹션 끝에 "출처" info-block으로 공인된 출처(정부기관·공인 학회·원 논문
  등)를 링크한다. 검색해서 나오는 게 개인 블로그뿐이면 링크 없이 넘어간다.
- 화면에 보이는 퍼센트 표기는 소수점 둘째 자리까지만 쓰고, 불필요한 뒤 0은 생략한다
  (예: 3.595% → 3.6%). 실제 계산에 쓰는 내부 값(`RATE` 등 JS 상수)은 원래 정확한 값을
  그대로 유지하고, 화면 표시 텍스트만 반올림한다 — 계산 로직을 건드리는 게 아니다.

## 파일 구조

- HTML은 저장소 루트, 스크립트는 `js/`, 스타일은 `style.css` 하나로 통합한다
  (계산기별로 CSS를 따로 만들지 않는다).
- `.claude/skills/calculator-info-section/`: 계산기 서브페이지(입력+결과+설명+광고슬롯)
  전용 구조 스킬. 계산기 페이지를 만들거나 손볼 땐 이 스킬을 먼저 로드한다.

## 공통 컴포넌트 — 계산기 페이지든 정보 페이지든 동일 규칙

- `criteria-table`은 기본이 숫자용(마지막 열 모노스페이스+우측정렬)이다. 문장이 들어가면
  `class="criteria-table text-table"`을 붙여 일반 한글 폰트+좌측정렬로 바꾼다.
- 기존 클래스(`info-block`, `criteria-table`, `method-compare-grid` 등)로 표현 가능한지
  먼저 확인하고, 정말 새 스타일이 필요할 때만 추가 전에 사용자에게 확인한다. 이 디자인은
  "영수증 느낌 빼줘", "가로 배치 유지" 같은 구체적 피드백을 여러 차례 거쳐 다듬어진
  것이라, 임의로 다른 톤을 시도하면 되돌리는 작업만 늘어난다.

## 새 페이지 추가 시 체크리스트

- **계산기 추가**: `.claude/skills/calculator-info-section` 스킬을 따라 만들고 —
  (1) 다른 모든 페이지의 사이드바 nav에 링크 추가, (2) `index.html` 카테고리 카드 추가,
  (3) 새 페이지 자신의 사이드바 링크에만 `class="active"`.
- **모든 새 페이지 공통**: footer에 소개(`about.html`)·문의(`contact.html`)·
  개인정보처리방침(`privacy.html`)·이용약관(`terms.html`) 링크 4개를 그대로 포함한다
  (기존 페이지의 footer 마크업을 복사).
- `index.html`에는 광고 슬롯(`.ad-slot`)을 넣지 않는다.
- 광고는 항상 계산 결과 아래에만 둔다. 계산기 UI 위쪽에는 넣지 않고, 자동광고·앵커·
  전면(인터스티셜) 광고는 쓰지 않는다.

## 절대 하지 않는 것 (법적 리스크)

- 금융상품 비교·추천·상담신청 기능 (금소법 §67 — 계산 결과만 보여주고 끝낸다)
- 대출 계산기에 대부업체 배너·상담 링크 (대부업법)
- "세금환급"·"절세전문"·"신고대행" 등의 문구 (세무사법). "종합소득세 계산기"처럼
  도구임이 분명한 표현은 쓰되 환급을 유도하는 문구는 쓰지 않는다.
- 계산기 페이지를 존재 이유 없이 대량 생성 (구글 Scaled Content Abuse 리스크) — 페이지
  하나하나가 왜 필요한지 설명할 수 있어야 한다.
- 면책 문구만으로 법적 방어가 된다고 보지 않는다. 실질적 방어는 출처·기준연도·갱신일을
  성실히 유지하는 것이다 (콘텐츠 원칙 참고).
